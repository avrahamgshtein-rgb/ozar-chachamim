#!/usr/bin/env bash
#
# Pre-commit hook: validate data and graph integrity
# Runs audit to detect errors (duplicate IDs, orphan connections).
# Exit status is propagated directly from the audit script.
#

cd "$(git rev-parse --show-toplevel)"
node scripts/audit-data-sync.mjs
exit $?
