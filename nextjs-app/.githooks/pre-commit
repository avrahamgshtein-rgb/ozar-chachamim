#!/usr/bin/env bash
#
# Pre-commit hook: validate data and graph integrity
# Runs audit with --check to detect errors and data drift.
# Exit status propagated directly: 0 = valid, 1 = errors or drift detected.
#

cd "$(git rev-parse --show-toplevel)"
node scripts/audit-data-sync.mjs --check
exit $?
