# מה מהסדנה (Session 3–5) שווה להטמיע באוצר חכמים

עברתי על תמלולי שלוש הסדנאות (סקילים, סאב-אייג'נטים, פלאגינים, זיכרון/CLAUDE.md, אותנטיקציה+זיכרון בסופרבייס, MCP, hooks, קרון/routines, סקיל עיצוב). הנה מיפוי ממוקד לפרויקט הספציפי שלנו — לא רשימת "מה זה סקיל" כללית, אלא מה בפועל כדאי לבנות כאן, מדורג לפי השפעה.

## מצב ביצוע — 2026-07-22

- ✅ **שלב 1 — ביקורת מקורות מלאה:** נסרקה רקורסיבית תיקיית Drive "חכמי ישראל". כל 423 מסמכי Google Docs הקנוניים מיוצגים מקומית בתוכן זהה; 0 חסרים ו־0 שיוכים שגויים.
- ✅ **שלב 2 — איחוד הנתונים המקומיים:** הגרף הקנוני כולל 409 רשומות ו־1,634 קשרים ייחודיים, לאחר מיפוי 44 מזהים כפולים. בדיקות החוזק מסתיימות עם 0 מזהים כפולים ו־0 קשרים יתומים.
- ✅ **שלב 3 — תהליך חוזר:** נוספו סקריפטים אידמפוטנטיים לייבוא מחקרי Drive, איחוד נתונים וביקורת סנכרון. כל שש מנות הייבוא נבדקו שוב ומחזירות 0 רשומות להוספה.
- ✅ **שלב 4 — Supabase החי:** המיגרציה הופעלה ואומתה. התצוגות החיות מחזירות בדיוק 409 חכמים ו־1,634 קשרים, כולל 17 קשרי `family`; אין מזהים חסרים, עודפים, אי־התאמות שמות או קשרים חסרים. נתוני ה־legacy ‏377/28/10 נשמרו וגם גובו, וטבלאות השכבה והגיבוי הוקשחו ב־RLS ללא הרשאות ציבוריות ישירות.
- 🟡 **שלב 5 — אוטומציה ומוצר אישי:** ה־pre-commit hook הותקן והופעל מול ביקורת הגרף; נארזו חמישה סקילים רשמיים (`sync-master-file`, `dedupe-sages`, `translate-research`, `research-aggregator`, `ozar-ui-design`); נוספו פקודות npm קבועות; ו־Milestone 2 חובר מקומית לטבלאות החיות הקיימות (`profiles`, `bookmarks`, `user_history`) עם כניסה בקישור חד־פעמי, סימניות, היסטוריית צפייה והערות אישיות. TypeScript ו־Next.js production build עוברים. נותרו שלוש פעולות חיצוניות: לקבע מועד ל־routine, לספק סביבת תרגום (`ANTHROPIC_API_KEY` והספרייה), ולאשר פריסה בנפרד.

דוח הראיות המלא נמצא ב־`data/DRIVE_RESEARCH_EMBEDDING_AUDIT_2026-07-21.md`.

---

## 1. תגלית דחופה שנחשפה תוך כדי הכנת המסמך הזה
**הפער נסגר.** לפני המיגרציה נמצאו במסד החי 377 חכמים ו־28 קשרים בלבד. לאחר preflight זוהו 120 מזהי־עבר ו־30 התנגשויות ID, ולכן הוקמה שכבה קנונית נפרדת במקום upsert מסוכן לטבלאות ה־legacy. התצוגות שהאתר קורא מחזירות כעת בדיוק 409 חכמים ו־1,634 קשרים, ונבדקו מול `data.json` ללא סטייה. השלב הבא הוא routine מתוזמן שמזהה פער ומתריע לפני סנכרון עתידי.

## 2. Skills לבנות לפרויקט הזה (במקום תהליכים ידניים חוזרים)
כל אחד מאלה הוא בדיוק תהליך שביצעתי **ידנית** הערב — סקיל הופך אותו לחוזר ועקבי:
- **`sync-master-file`** — ליבת המימוש כבר קיימת ב־`scripts/sync-public-data.mjs` וב־`scripts/audit-data-sync.mjs`: שימור מזהים, איחוד מקורות, מיפוי כפילויות ולידציית קשרים. נותר לארוז אותה כסקיל רשמי עם ממשק קצר.
- **`translate-research`** — עטיפה של `translate_research_v2.py` הקיים כסקיל רשמי. ספירת המצב הנוכחית היא 346 קובצי מחקר עבריים, ורק 2 קובצי EN ו־2 קובצי RU; לכן יש לבצע תור תרגום חדש על בסיס אינדקס המחקר המעודכן ולא להסתמך על ספירת 207 הישנה.
- **`dedupe-sages`** — ליבת המימוש הושלמה מקומית: 44 מזהים כפולים ממופים ב־`data/sage-id-aliases-2026-07-21.json`, והסנכרון חוזר ללא שינוי. נותר להפוך את הבדיקה לסקיל תחזוקה חוזר.

## 3. Sub-agents — מה עדיין מצדיק אייג'נט
בקובץ CLAUDE.md כבר רשומה רשימת "Potential Agents" — Research Aggregator, Connection Validator, Migration Path Tracer — אבל אף אחד מהם לא נבנה בפועל. לפי מה שנלמד בסדנה (Skill = מתכון קטן, Agent = "השף" שמנהל תהליך שלם באוטונומיה), אלה מועמדים טובים להפוך לאייג'נטים אמיתיים:
- **Connection Validator** — החלק הדטרמיניסטי כבר מומש ב־`scripts/audit-data-sync.mjs`, והוא מחזיר כעת 0 קשרים יתומים. עדיף להשאיר את בדיקת השלמות כסקריפט קשיח ומהיר, ולתת לאייג'נט להיכנס רק כאשר נדרש להסביר או לתקן חריגה.
- **Research Aggregator** — קורא מסמכי docx חדשים מ-`sources/`, מחלץ תאריכים/מיקומים, מציע שיוך למזהה חכם קיים.

## 4. זיכרון (CLAUDE.md) — מצב נבדק
בדיקה חוזרת מראה שהטענה הישנה כבר אינה תקפה: ב־`CLAUDE.md` קיים כעת מופע אחד של "Session 3 Framework", ו־`MEMORY.md` קיים ומתעדכן. אין כאן עוד פעולת תיקון דחופה; נותר רק להמשיך ביקורת תקופתית על עובדות שהתיישנו.

## 5. אותנטיקציה + זיכרון אישי (סופרבייס) — כבר בתהליך!
זה בדיוק התבנית שנלמדה בסדנה ("Budget Tracker" עם אותנטיקציה + זיכרון אישי דרך Supabase+Vercel). נכון ל־2026-07-22, **Milestone 2 ממומש מקומית** על גבי הטבלאות החיות שכבר היו קיימות: כניסה בדוא״ל, סימניות אישיות, רישום היסטוריית צפייה והערות פרטיות. לא נדרש שינוי סכמת מסד. ההפעלה לציבור תלויה בפריסה נפרדת ובהגדרת `NEXT_PUBLIC_SUPABASE_URL` ו־`NEXT_PUBLIC_SUPABASE_ANON_KEY` בסביבת האירוח.

## 6. MCP — חיבור לשירותים חיצוניים
בסדנה הודגם MCP לחיבור לחשבון בנק/אשראי בפועל. אצלנו יש שני מועמדים טבעיים:
- **Sefaria MCP** — הבדיקה המעודכנת מצאה שני שרתים רשמיים של Sefaria: Developers MCP ו־Texts MCP. הם אינם מותקנים בסביבת Codex הנוכחית, ולכן לא הוצג חיבור מדומה. החיבור דורש הוספה ידנית כ־Custom Connector בחשבון המשתמש; עד אז הכפתור באתר ממשיך לפתוח את Sefaria בקישור חיצוני בטוח.
- ה־API הרשמי של Sefaria אינו דורש מפתח, ולכן אפשר בעתיד להוסיף תצוגת מקורות inline בנפרד ממחבר ה־MCP, לאחר הגדרת חוויית המשתמש והיקף הציטוטים.

## 7. Hooks — בדיקות אוטומטיות לפני commit
הושלם: `core.hooksPath` המקומי מצביע ל־`.githooks`, וה־pre-commit מריץ `scripts/precommit-check.mjs`, שעוטף את `audit-data-sync.mjs --check`. הבדיקה הופעלה ומחזירה 0 מזהים כפולים ו־0 קשרים יתומים.

## 8. סקיל עיצוב (Front-End Design)
בסדנה הודגם סקיל עיצוב שקלוד מפעיל **מעצמו** כשמבקשים "עיצוב". הפרויקט הזה כבר בעל שפה עיצובית עקבית (glass/gold/ink tokens ב-globals.css) — אפשר לקודד אותה כסקיל פרויקט-ספציפי, כך שכל רכיב UI חדש (למשל LoadingState/EmptyState שתוכננו ב-Phase 3 בעבר ולא מומשו) ייצא עקבי מהיום הראשון בלי שצריך להזכיר את הפלטה בכל בקשה.

---

## סדר עדיפויות מומלץ מכאן

1. ✅ **מיגרציה וסנכרון Supabase↔data.json** — הושלם ואומת מול כל המזהים, השמות והקשרים.
2. 🟡 **routine ביקורת שבועי** — פקודות הבדיקה מוכנות ומאומתות; יצירת המשימה ממתינה לאישור המועד המוצע (יום ראשון, 08:00 שעון ישראל).
3. 🟡 **סקיל `translate-research`** — נארז ונבדק עם תור מוגבל ואידמפוטנטי. מצב נוכחי: 346 בסיס, 2 EN, ‏2 RU, ולכן 344 חסרים בכל שפה. הרצה בפועל חסומה עד שתהיה ספריית `anthropic` ומפתח תהליך, בלי לשמור סוד בקוד.
4. ✅ **pre-commit hook לוולידציית FK וכפילויות** — הותקן, חובר והופעל בהצלחה.
5. ✅ **אריזת `sync-master-file` ו־`dedupe-sages` כסקילים** — הושלמה יחד עם `research-aggregator` ו־`ozar-ui-design`.
6. ✅ **Milestone 2** — ממומש מקומית ללא שינוי סכמת מסד; ממתין לפריסה נפרדת.
7. ✅ **Research Aggregator** — נארז כתהליך שמטפל רק בדלתא של מקורות חדשים ושומר את קו־הבסיס של 423 מסמכים.

השלב המעשי הבא הוא להפעיל את ה־routine במועד מאושר; אחריו אפשר להריץ תרגום בקבוצות קטנות ברגע שסביבת הספק זמינה, ולבסוף לפרוס את ממשק Milestone 2 באישור מפורש.
